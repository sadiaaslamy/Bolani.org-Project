function mult(value) {
    var x = 0.5 * value;
    document.getElementById('amount').value =  x ;
  }

  function mult2(value) {
    var y = value / 0.5;
    document.getElementById('number').value = y;
  }
      

